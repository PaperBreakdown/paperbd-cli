# install-paperbd

Bootstrap installer for PaperBD.

Run:

```bash
npx install-paperbd@latest
```

It will:

- install or upgrade the global `paperbd` CLI
- install the PaperBD skill
- print the main commands to get started
- optionally run `paperbd login`
